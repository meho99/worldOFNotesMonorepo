{
  "name": "root",
  "private": true,
  "scripts": {
    "build-types": "lerna run build:watch",
    "start-web": "cd packages/web-app && BROWSER=none npm start && run build-types"
  },
  "workspaces": {
    "packages": [
      "packages/*"
    ]
  },
  "devDependencies": {
    "@testing-library/react": "^11.2.0",
    "lerna": "^3.22.1"
  }
}
